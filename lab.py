# Codes here is to test every logic before it goes to any module
from helpers.app_config import loadAppSettings

app_config = loadAppSettings()
print(app_config)


# DEFINING STANDARD CODES
# grades, personalities, dailyActivities, favoriteSubjects, ambitions, favoritePlays = getLatestAttributeCodes(cursor)

# import tkinter as tk
# r = tk.Tk()
# r.title('Counting Seconds')
# button = tk.Button(r, text='Close window', width=25, command=r.destroy)
# button.pack()
# r.mainloop()

